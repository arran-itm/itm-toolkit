# rsync
```bash
rsync -rvh --progress source/ destination/
```

- -a = archive mode (true backup/mirror - only use for proper backup keeping permissions)
- -v = verbose
- -h = human readable sizes
- --progress = shows progress
- -r = recursive